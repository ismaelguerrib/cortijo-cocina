import { MigrationInterface, QueryRunner } from 'typeorm'; export class CreateRecipeVotes1750000001000 implements MigrationInterface { async up(q: QueryRunner): Promise<void> { await q.query(`CREATE TABLE "recipe_votes" ("id" uuid NOT NULL DEFAULT uuid_generate_v4(), "recipe_id" uuid NOT NULL, "member" varchar(40) NOT NULL, "score" smallint NOT NULL, "created_at" TIMESTAMP NOT NULL DEFAULT now(), CONSTRAINT "UQ_recipe_votes_recipe_member" UNIQUE ("recipe_id", "member"), CONSTRAINT "PK_recipe_votes_id" PRIMARY KEY ("id"), CONSTRAINT "FK_recipe_votes_recipe" FOREIGN KEY ("recipe_id") REFERENCES "recipes"("id") ON DELETE CASCADE)`); } async down(q: QueryRunner): Promise<void> { await q.query('DROP TABLE "recipe_votes"'); } }
