import { MigrationInterface, QueryRunner } from 'typeorm'; export class CreateRecipePhotos1750000002000 implements MigrationInterface { async up(q: QueryRunner): Promise<void> { await q.query(`CREATE TABLE "recipe_photos" ("id" uuid NOT NULL DEFAULT uuid_generate_v4(), "recipe_id" uuid NOT NULL, "storage_key" varchar(255) NOT NULL, "content_type" varchar(100) NOT NULL, "created_at" TIMESTAMP NOT NULL DEFAULT now(), CONSTRAINT "UQ_recipe_photos_storage_key" UNIQUE ("storage_key"), CONSTRAINT "PK_recipe_photos_id" PRIMARY KEY ("id"), CONSTRAINT "FK_recipe_photos_recipe" FOREIGN KEY ("recipe_id") REFERENCES "recipes"("id") ON DELETE CASCADE)`); } async down(q: QueryRunner): Promise<void> { await q.query('DROP TABLE "recipe_photos"'); } }
