import { MigrationInterface, QueryRunner } from 'typeorm'; export class CreateVacations1750000003000 implements MigrationInterface { async up(q: QueryRunner): Promise<void> { await q.query(`CREATE TABLE "vacations" ("id" uuid NOT NULL DEFAULT uuid_generate_v4(), "name" varchar(120) NOT NULL, "starts_on" date NOT NULL, "ends_on" date NOT NULL, "created_at" TIMESTAMP NOT NULL DEFAULT now(), "updated_at" TIMESTAMP NOT NULL DEFAULT now(), CONSTRAINT "PK_vacations_id" PRIMARY KEY ("id"))`); await q.query('ALTER TABLE "meal_assignments" ADD "vacation_id" uuid'); await q.query('ALTER TABLE "meal_assignments" ADD CONSTRAINT "FK_meal_assignments_vacation" FOREIGN KEY ("vacation_id") REFERENCES "vacations"("id") ON DELETE SET NULL'); } async down(q: QueryRunner): Promise<void> { await q.query('ALTER TABLE "meal_assignments" DROP CONSTRAINT "FK_meal_assignments_vacation"'); await q.query('ALTER TABLE "meal_assignments" DROP COLUMN "vacation_id"'); await q.query('DROP TABLE "vacations"'); } }
