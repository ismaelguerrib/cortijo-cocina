import { Injectable } from '@nestjs/common'; import { InjectRepository } from '@nestjs/typeorm'; import { Repository } from 'typeorm'; import { CreateVacationDto } from './dto/create-vacation.dto'; import { VacationEntity } from './entities/vacation.entity'; @Injectable() export class VacationsService { constructor(@InjectRepository(VacationEntity) private readonly vacations: Repository<VacationEntity>) {} findAll(): Promise<VacationEntity[]> { return this.vacations.find({ order: { startsOn: 'DESC' } }); } create(dto: CreateVacationDto): Promise<VacationEntity> { return this.vacations.save(this.vacations.create({ ...dto, name: dto.name.trim() })); } }
