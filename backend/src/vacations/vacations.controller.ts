import { Body, Controller, Get, Post } from '@nestjs/common'; import { CreateVacationDto } from './dto/create-vacation.dto'; import { VacationEntity } from './entities/vacation.entity'; import { VacationsService } from './vacations.service'; @Controller('vacations') export class VacationsController { constructor(private readonly vacations: VacationsService) {} @Get() findAll(): Promise<VacationEntity[]> { return this.vacations.findAll(); } @Post() create(@Body() dto: CreateVacationDto): Promise<VacationEntity> { return this.vacations.create(dto); } }
